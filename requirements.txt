# Python runtime
# Use Python 3.10-3.12 for this backend. The embedding stack pulled in by
# sentence-transformers/torch may not have stable Windows wheels for Python 3.14.

# Web API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Environment and validation
python-dotenv>=1.0.1
pydantic>=2.7.0

# Agent orchestration and LLM providers
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Vector database and semantic search
lancedb>=0.14.0
pyarrow>=15.0.0
sentence-transformers>=3.0.0

# Project-local note:
# backend/medical_specialties.py imports fdr.config.medical_specialties.
# I did not add an "fdr" pip package because that module does not appear to map
# to a public dependency for this codebase; restore/add it as local project code
# or replace the import before using those modules.
